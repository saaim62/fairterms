services:
  api:
    build:
      context: ./apps/api
    ports:
      - "8000:8000"
    volumes:
      - ./apps/api:/app
    environment:
      - FAIRTERMS_CORS_ORIGINS=
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload
